# Cross Player 1.1.2

This patch release improves the mobile player empty state.

Highlights:
- Replaced the browser's default empty video placeholder with a cleaner in-app idle state.
- Added a more appropriate “Nothing is playing” screen when no media is active on mobile.
- Kept the existing audio placeholder behavior for audio playback.
